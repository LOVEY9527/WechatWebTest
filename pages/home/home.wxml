<!--home.wxml-->
<view class="container">
    <view bindTap="viewTap">点我</view>
    <view bindtap="tapTest">点我</view>    
    <!--<view>{{text}}</view>
    <block wx:for="{{array}}" wx:for-index="i" wx:for-item="obj">
        <view>{{obj.key}}</view>
    </block>   -->
</view>